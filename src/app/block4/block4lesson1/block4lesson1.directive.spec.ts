import { Block4lesson1Directive } from './block4lesson1.directive';

describe('Block4lesson1Directive', () => {
  it('should create an instance', () => {
    const directive = new Block4lesson1Directive();
    expect(directive).toBeTruthy();
  });
});
