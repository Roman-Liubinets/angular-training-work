import { Block5lesson2Pipe } from './block5lesson2.pipe';

describe('Block5lesson2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Block5lesson2Pipe();
    expect(pipe).toBeTruthy();
  });
});
